{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ],
    "rewrites": [ { "source": "/(.*)", "destination": "/" } ],
    "buildCommand": "pnpm install --no-frozen-lockfile && pnpm build"
}
  